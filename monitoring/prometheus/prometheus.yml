global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "tsp-solver"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["tsp-solver:8000"]
        labels:
          service: "tsp-solver"
          environment: "development"
